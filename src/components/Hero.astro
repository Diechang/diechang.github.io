<section
  id="about"
  class="relative min-h-screen flex items-center justify-center overflow-hidden pt-16"
>
  <!-- Background Images with Parallax -->
  <div class="absolute inset-0 w-full h-full z-0">
    <!-- Light Mode Background -->
    <div
      class="absolute inset-0 w-full h-full bg-cover bg-center opacity-50 dark:opacity-0 transition-opacity duration-500 parallax-bg"
      style="background-image: url('/img/kanazawa_station_day2.jpg');"
    >
    </div>
    <!-- Dark Mode Background -->
    <div
      class="absolute inset-0 w-full h-full bg-cover bg-center opacity-0 dark:opacity-50 transition-opacity duration-500 parallax-bg"
      style="background-image: url('/img/kanazawa_station_night1.jpg');"
    >
    </div>
    <!-- Overlay Gradient for better text readability -->
    <div
      class="absolute inset-0 bg-gradient-to-b from-bg/80 via-bg/50 to-bg z-10"
    >
    </div>
  </div>

  <!-- Content -->
  <div class="relative z-20 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Icon -->
    <div class="mb-8 flex justify-center">
      <img
        src="/img/icon.png"
        alt="Daisuke Yokogawa Icon"
        class="w-32 h-32 rounded-full border-4 border-surface shadow-xl"
      />
    </div>

    <h1 class="text-4xl sm:text-5xl md:text-7xl font-bold tracking-tight mb-8">
      <span
        class="block text-lg sm:text-xl md:text-2xl text-text-main mb-4 font-medium tracking-wide"
      >
        石川県・金沢市を中心に、全国のWeb開発を支えるフロントエンドエンジニア
      </span>
      <span
        class="block text-4xl sm:text-5xl md:text-6xl text-transparent bg-clip-text bg-gradient-to-r from-primary to-accent mb-6 pb-2"
      >
        Daisuke Yokogawa
      </span>
    </h1>

    <p
      class="mt-4 text-lg sm:text-xl text-text-muted max-w-3xl mx-auto mb-10 leading-relaxed"
    >
      デザインからコーディング、フロントエンド、バックエンドまで一貫して対応。<br
        class="hidden sm:block"
      />
      工程を横断した理解と実装力で、スムーズな進行と確かな品質を提供します。
    </p>

    <div class="flex justify-center gap-4">
      <a
        href="#works"
        class="px-8 py-3 rounded-full bg-primary text-white font-medium hover:bg-primary/90 transition-all shadow-lg shadow-primary/25"
      >
        制作実績
      </a>
      <a
        href="#contact"
        class="px-8 py-3 rounded-full bg-surface text-text-main border border-text-muted/20 hover:border-primary/50 transition-all"
      >
        お問い合わせ
      </a>
    </div>
  </div>
</section>

<script>
  // Parallax Effect
  document.addEventListener("scroll", () => {
    const scrollPosition = window.scrollY;
    const parallaxBgs = document.querySelectorAll(".parallax-bg");

    parallaxBgs.forEach((bg) => {
      // Move background at a slower speed than scroll
      const speed = 0.5;
      (bg as HTMLElement).style.transform =
        `translateY(${scrollPosition * speed}px)`;
    });
  });
</script>
